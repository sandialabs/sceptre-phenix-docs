name: Lint

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prek:
    name: prek
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          persist-credentials: false
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.12'
      - name: Install prek
        run: pip install 'prek>=0.4.3'
      - name: Validate prek config
        run: prek validate-config prek.toml
      - name: Run prek
        # The 'manual' stage runs every hook except those scoped to local git
        # stages (e.g. branch name validation, which would fail on the 'main'
        # push event GitHub emits after a PR merge).
        run: prek run --all-files --hook-stage manual --show-diff-on-failure
